spring:
  application:
    name: card-client
  cloud:
    config:
      uri: http://localhost:8888
  data:
    mongodb:
      uri: mongodb://localhost:27017/card
management:
  security:
    enable: false
